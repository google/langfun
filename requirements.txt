anyio>=4.7.0
jinja2>=3.1.2
mcp>=1.17.0
puremagic>=1.20
# Floor must expose pg.utils.json_conversion.enable_opaque_pickle, used by the
# trusted-boundary opt-in guards for secure-by-default opaque pickle
# (b/511887449). First nightly with that API is 0.5.0.dev202606030947.
pyglove>=0.5.0.dev202606030947
requests>=2.31.0

# extras:vertexai
google-auth>=2.16.0

# extras:mime-pil
pillow>=10.0.0

# extras:ui
termcolor==1.1.0
tqdm>=4.64.1
